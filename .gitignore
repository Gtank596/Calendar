calendar-events.json
*.bak
.DS_Store
Thumbs.db

# Secrets — never commit these
.env
.env.*
*.key
*.pem

# Quality Foundation V1 — dev tooling artifacts (never commit)
node_modules/
coverage/
test-results/
playwright-report/
